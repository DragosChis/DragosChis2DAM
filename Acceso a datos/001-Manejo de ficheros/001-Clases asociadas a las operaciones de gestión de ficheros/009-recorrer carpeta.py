import os #importamos la libreria del sistema

lista = os.listdir("fotos") #Creamos una lista a la que asignamos el directorio fotos

print(lista) #Mostramos en pantalla la lista, el contenido de la carpeta fotos
